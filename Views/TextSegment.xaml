<UserControl x:Class="WEBPMemeGenerator.TextSegmentControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WEBPMemeGenerator"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Label Content="Text:" VerticalAlignment="Center" Margin="0,0,8,0"/>
        <TextBlock Text="{Binding Text, UpdateSourceTrigger=PropertyChanged}"
                   VerticalAlignment="Center"
                   Grid.Column="1"
                   Margin="0,0,8,0"/>
        <xctk:IntegerUpDown Value="{Binding FontSize, UpdateSourceTrigger=PropertyChanged}"
                                Minimum="1"
                                Maximum="200"
                                VerticalAlignment="Center"
                                Grid.Column="2"
                                Margin="0,0,8,0"/>
    </Grid>
</UserControl>
