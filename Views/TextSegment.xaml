<UserControl x:Class="WEBPMemeGenerator.TextSegmentControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WEBPMemeGenerator"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Label Content="Text:"/>

        <TextBox Text="{Binding Text, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

        <xctk:IntegerUpDown Value="{Binding StartFrame, UpdateSourceTrigger=PropertyChanged}"
                    Minimum="1"
                    Maximum="{Binding MaxFrame}" />

        <xctk:IntegerUpDown Value="{Binding StopFrame, UpdateSourceTrigger=PropertyChanged}"
                    Minimum="1"
                    Maximum="{Binding MaxFrame}" />
    </Grid>
</UserControl>
